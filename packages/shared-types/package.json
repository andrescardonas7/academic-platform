{
  "name": "@academic/shared-types",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc",
    "build:vercel": "npx tsc --project tsconfig.vercel.json",
    "dev": "npx tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@academic/tsconfig": "workspace:*"
  }
}
